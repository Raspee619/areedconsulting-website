{
  "name": "areedconsulting-website",
  "version": "1.0.0",
  "description": "Backend server for A Reed Consulting website – serves static files and handles Stripe Checkout sessions",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^4.21.2",
    "stripe": "^17.5.0"
  }
}
